#include <stdio.h>
#include <stdlib.h>

int main (void) {
	int n;
		do {
		printf("Informe um numero positivo N (negativo para sair): ");
		scanf("%d",&n);
		if(n > 0) {
			// TODO: 
		}
	} while (n > 0);

	printf("Fim do programa!\n");
	return 0;
}	